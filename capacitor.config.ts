import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'techstack-onboarding',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
